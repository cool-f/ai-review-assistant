# FastAPI 核心
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# 数据库
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.30,<1.0
pgvector>=0.3,<1.0

# 迁移
alembic>=1.13,<2.0

# 配置与校验
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# AI 调用（通用接口）
httpx>=0.27,<1.0
openai>=1.0,<2.0
anthropic>=0.39,<1.0

# 数值计算（pgvector 依赖，Python 3.13 需 >=2.1）
numpy>=2.1,<2.2

# Token 计数
tiktoken>=0.7,<1.0

# 中文分词 (知识点匹配)
jieba>=0.42,<1.0

# 文件解析
python-pptx>=0.6,<1.0
python-docx>=1.0,<2.0
PyMuPDF>=1.24,<2.0
